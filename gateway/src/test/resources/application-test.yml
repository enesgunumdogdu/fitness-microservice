spring:
  cloud:
    config:
      enabled: false
    gateway:
      routes: []
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8181/auth/realms/test

eureka:
  client:
    enabled: false
